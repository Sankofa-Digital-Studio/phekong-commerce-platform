# Cypress status

Cypress is already installed and configured on this branch.

Available commands:

```bash
npm run e2e:cypress:open
npm run test:e2e:cypress
```

Configuration:

```text
cypress.config.ts
```

Current specs:

```text
cypress/e2e/about.cy.ts
```

Run the Next.js development server first:

```bash
npm run dev
```

Then execute Cypress in a second terminal.
