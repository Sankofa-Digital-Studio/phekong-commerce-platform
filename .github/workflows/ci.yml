name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fbd5faf # v4.1.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - name: Install Chromium for browser tests
        run: npx playwright install --with-deps chromium
      - run: npm run test:run
      - run: npm run build
      - name: Build static Storybook
        run: npm run build-storybook

  supabase-baseline:
    name: Supabase M0 validation
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fbd5faf # v4.1.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Confirm repository-pinned Supabase CLI
        run: npm run supabase:version
      - name: Validate migrations, access boundaries and booking conflicts
        run: npm run supabase:m0:ci